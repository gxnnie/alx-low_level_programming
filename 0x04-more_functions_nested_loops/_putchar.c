#include <unlsted.h>
#include "main.h"

/**
 * _putchar - writes the characer c tostdout
 * @c: the char to print
 * Return: On success 1 else -1
 */
int _putchar(char c)
{
	return (write(1, &c, i));
}

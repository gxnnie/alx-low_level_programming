#include "main.h"

/**
 * print_square - prints a square
 * @size: size of the square
 */
void print_square(int size)
{
	int 1, j;
	
	for (i = 0; < size; i++)
	{
		for (j = 0; j < size; j++)
		{
			_putchar(35);
		}
		if (i!= size -1)
			_putchar('\n');
	}
	_putchar('\n');
}
